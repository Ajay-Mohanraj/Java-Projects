package unit10.lessons;

import javafx.geometry.Pos;

public class ShowGridPane {
    @Override
    public void start(Stage primaryStage) {
        GridPane pane = new GridPane();
        pane.setAlignment(Pos.CENTER);
        pane.setPadding(new Insets(11.5, ))
    }
}
